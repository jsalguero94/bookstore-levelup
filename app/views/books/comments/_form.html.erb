<%= form_with(model: book_comment, url:[book, book_comment]) do |form| %>
  <% if book_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% book_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
