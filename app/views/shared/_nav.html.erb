Welcome!    <%=  user_signed_in? ? (current_user.first_name() +' '+ current_user.last_name()) : ''%>
|   <%= link_to "Home", root_path %>
|   <%= link_to "Books", books_path %>
|   <%= link_to "Categories", categories_path %>
|   <%= link_to "Tags", tags_path %>
|   <%= link_to "Request a book", requests_path %>
|   <%= link_to 'Edit Profile', edit_user_registration_path %>
|   <%= button_to 'Sign Out', destroy_user_session_path, method: :delete,
    form: {data: {turbo_confirm: 'Are you sure?'}} %>
  <%= link_to t('navigation.sign_out'), destroy_user_session_path, data: { "turbo-method": :delete }, class: "btn btn-danger ml-3" %>

<hr>
